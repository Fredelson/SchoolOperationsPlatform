import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#1E3A8A",
    },
    secondary: {
      main: "#2563EB",
    },
    background: {
      default: "#F8FAFC",
    },
  },
});

export default theme;
