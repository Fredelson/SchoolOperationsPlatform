// ============================================
// ARAB UNITY SCHOOL
// Card Theme Overrides
// ============================================

export const cardOverrides = {
  MuiCard: {
    styleOverrides: {
      root: ({ theme }) => ({
        borderRadius: theme.shape.borderRadius,
      }),
    },
  },
};
