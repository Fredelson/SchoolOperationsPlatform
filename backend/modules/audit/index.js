/* =========================================================
   Audit Module Entry Point
========================================================= */

const activityLogger = require("./services/activityLogger");

module.exports = {
  activityLogger,
};